[project]
name = "pycarinthia-website"
version = "0.1.0"
description = "Pelican static site for PyCarinthia, the Python user group in Carinthia."
requires-python = ">=3.11"
dependencies = [
  "pelican[markdown]>=4.11,<5",
  "python-dateutil>=2.9,<3",
]

[tool.uv]
package = false
